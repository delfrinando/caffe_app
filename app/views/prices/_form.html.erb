<%= form_for(price) do |f| %>
  <% if price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_drinks %>
    <%= f.text_field :id_drinks %>
  </div>

  <div class="field">
    <%= f.label :tall %>
    <%= f.text_field :tall %>
  </div>

  <div class="field">
    <%= f.label :grande %>
    <%= f.text_field :grande %>
  </div>

  <div class="field">
    <%= f.label :venti %>
    <%= f.text_field :venti %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
